# Buck2 outputs
/buck-out
buck-out/
.buck-out/
scripts/package-binary-rs/target


# Installer-generated files (regenerated per-install)
/install/BUCK

# Prelude (external)
prelude/

# Downloaded sources
downloads/
distfiles/
.vendor
vendor

# Build artifacts
*.o
*.a
*.so
*.ko

# Python / pytest
__pycache__/
.pytest_cache/
.venv/

# Editor files
*~
*.swp
.vscode/
.idea/

# OS files
.DS_Store
Thumbs.db
.buckconfig.local

# Prebuilt toolchain archives (build artifacts, not source)
prebuilt/*.tar.*
tc/prebuilt/*.tar.*

# Private patch registry (stubs committed via git add -f, user overrides ignored)
patches/**/*.patch
patches/BUCK
