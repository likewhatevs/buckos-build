# Buck2 outputs
/buck-out
buck-out/
.buck-out/
scripts/package-binary-rs/target


# Installer-generated files (regenerated per-install)
/config/use_config.bzl
/install/BUCK

# Prelude (external)
prelude/

# Downloaded sources
downloads/
distfiles/
.vendor
vendor

# Build artifacts
*.o
*.a
*.so
*.ko

# Editor files
*~
*.swp
.vscode/
.idea/

# OS files
.DS_Store
Thumbs.db
.buckconfig.local

# Private patch registry (stubs committed via git add -f, user overrides ignored)
patches/**/*.patch
patches/registry.bzl
patches/BUCK
