load("//defs:package.bzl", "package")

# Compatibility layers - Wine and DirectX translation

filegroup(
    name = "compatibility",
    srcs = [
    ],
    default_target_platform = "//platforms:linux-target",
    visibility = ["PUBLIC"],
)

# Proton-like stack (DXVK + VKD3D)
filegroup(
    name = "proton-stack",
    srcs = [
    ],
    default_target_platform = "//platforms:linux-target",
    visibility = ["PUBLIC"],
)
