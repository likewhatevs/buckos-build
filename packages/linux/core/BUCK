# Core system libraries and utilities
#
# Filegroup aggregation doesn't work with package directory outputs
# (all named "installed") — Buck2 rejects duplicate short_paths.
# Use direct deps instead of these convenience groups.
