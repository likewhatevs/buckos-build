#
# Example: Raspberry Pi 4 Hardware Configuration
#
# Note: This is an example showing how you would structure
# hardware-specific fragments for ARM64 devices.
#

# ARM64 settings (would need different base config)
# CONFIG_ARM64=y

# BCM2711 SoC support
# CONFIG_ARCH_BCM2835=y

# SD/MMC
CONFIG_MMC=y
CONFIG_MMC_SDHCI=y
CONFIG_MMC_SDHCI_PLTFM=y

# USB (DWC2 for Pi)
CONFIG_USB_DWC2=y

# GPU (VideoCore)
CONFIG_DRM_VC4=y

# PWM
CONFIG_PWM=y
CONFIG_PWM_BCM2835=y

# GPIO
CONFIG_GPIO_BCM2835=y
CONFIG_PINCTRL_BCM2835=y
