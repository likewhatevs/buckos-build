load("//defs:package.bzl", "package")

# FIXME: pyropus.ca has redirect loop - needs working URL
genrule(
    name = "memtester",
    out = "memtester-disabled.txt",
    cmd = "echo 'Package disabled: upstream URL has redirect loop' > $OUT",
)
