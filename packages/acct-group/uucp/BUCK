load("//defs/rules:acct.bzl", "acct_group_package")

acct_group_package(
    name = "uucp",
    gid = 14,  # Standard uucp group GID (for serial devices)
    description = "System group for serial port access",
)
