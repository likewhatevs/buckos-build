load("//defs:package_defs.bzl", "acct_group_package")

acct_group_package(
    name = "usb",
    gid = 85,  # Standard USB group GID
    description = "System group for USB device access",
    visibility = ["PUBLIC"],
)
