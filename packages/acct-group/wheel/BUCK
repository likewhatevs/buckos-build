load("//defs:package_defs.bzl", "acct_group_package")

acct_group_package(
    name = "wheel",
    gid = 10,  # Standard wheel group GID
    description = "System group for administrative privileges",
    visibility = ["PUBLIC"],
)
