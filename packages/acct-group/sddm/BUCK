load("//defs:package_defs.bzl", "acct_group_package")

acct_group_package(
    name = "sddm",
    gid = 365,  # Standard SDDM group GID
    description = "System group for SDDM display manager",
    visibility = ["PUBLIC"],
)
