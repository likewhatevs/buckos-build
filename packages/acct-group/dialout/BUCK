load("//defs:package_defs.bzl", "acct_group_package")

acct_group_package(
    name = "dialout",
    gid = 20,  # Standard dialout group GID
    description = "System group for modem and serial port access",
    visibility = ["PUBLIC"],
)
