load("//defs:package_defs.bzl", "acct_group_package")

acct_group_package(
    name = "audio",
    gid = 18,  # Standard audio group GID
    description = "System group for audio device access",
    visibility = ["PUBLIC"],
)
