load("//defs:package_defs.bzl", "acct_group_package")

acct_group_package(
    name = "lp",
    gid = 7,  # Standard lp (line printer) group GID
    description = "System group for printer access",
    visibility = ["PUBLIC"],
)
