load("//defs/rules:acct.bzl", "acct_group_package")

acct_group_package(
    name = "render",
    gid = 98,  # Standard render group GID
    description = "System group for GPU render node access",
)
