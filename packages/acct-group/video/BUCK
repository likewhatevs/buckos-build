load("//defs:package_defs.bzl", "acct_group_package")

acct_group_package(
    name = "video",
    gid = 27,  # Standard video group GID
    description = "System group for video device access",
    visibility = ["PUBLIC"],
)
