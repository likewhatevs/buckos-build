load("//defs:package_defs.bzl", "acct_group_package")

acct_group_package(
    name = "kvm",
    gid = 78,  # Standard kvm group GID
    description = "System group for KVM virtualization access",
    visibility = ["PUBLIC"],
)
