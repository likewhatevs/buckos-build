"""
BXL graph structure test: cross-reference label assignment.

Run:
    buck2 bxl buckos//tests/graph/test_graph.bxl:test_graph
"""

load("//tests/graph:test_graph_impl.bzl", "run")

def _impl(ctx):
    passed, failed, details = run(ctx)
    if failed > 0:
        fail("{} assertion(s) failed\n{}".format(failed, details))

test_graph = bxl_main(
    doc = "Cross-reference label assignment on compile, prebuilt, image, bootscript, and config targets.",
    impl = _impl,
    cli_args = {},
)
