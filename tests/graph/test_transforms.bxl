"""
BXL graph structure test: transform chain wiring.

Run:
    buck2 bxl buckos//tests/graph/test_transforms.bxl:test_transforms
"""

load("//tests/graph:test_transforms_impl.bzl", "run")

def _impl(ctx):
    passed, failed, details = run(ctx)
    if failed > 0:
        fail("{} assertion(s) failed\n{}".format(failed, details))

test_transforms = bxl_main(
    doc = "Verify transform chain wiring in migrated packages.",
    impl = _impl,
    cli_args = {},
)
