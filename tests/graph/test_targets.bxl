"""
BXL graph structure test: target universe validation.

Run:
    buck2 bxl buckos//tests/graph/test_targets.bxl:test_targets
"""

load("//tests/graph:test_targets_impl.bzl", "run")

def _impl(ctx):
    passed, failed, details = run(ctx)
    if failed > 0:
        fail("{} assertion(s) failed\n{}".format(failed, details))

test_targets = bxl_main(
    doc = "Verify target universe parses and definition cells load cleanly.",
    impl = _impl,
    cli_args = {},
)
