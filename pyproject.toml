[project]
name = "buckos-tests"
version = "0.0.1"
requires-python = ">=3.11"

[dependency-groups]
dev = ["pytest>=8", "pytest-timeout>=2.2"]

[tool.pytest.ini_options]
testpaths = ["tests"]
markers = ["slow: tests that invoke buck2 build (deselect with -m 'not slow')"]
timeout = 300
